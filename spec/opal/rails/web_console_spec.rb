require 'spec_helper'

describe Opal::Rails::WebConsole do
  it 'has a version number' do
    expect(Opal::Rails::WebConsole::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
